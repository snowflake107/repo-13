apiVersion: v1
kind: Secret
metadata:
  name: {{ include "api-gateway.fullname" . }}-secret
  labels:
  {{- include "api-gateway.labels" . | nindent 4 }}
data:
  {{- with .Values.frontegg }}
  FRONTEGG_AUTH_PUBLIC_KEY: {{ required "A valid .Values.frontegg.authenticationPublicKey entry required!" .authenticationPublicKey }}
  FRONTEGG_CLOUD_API_GATEWAY_URL: {{ required "A valid .Values.frontegg.apiGatewayUrl entry required!" .apiGatewayUrl }}
  FRONTEGG_AUTHENTICATION_SERVICE_URL: {{ required "A valid .Values.frontegg.authenticationServiceUrl entry required!" .authenticationServiceUrl }}
  FRONTEGG_METADATA_SERVICE_URL: {{ required "A valid .Values.frontegg.metadataServiceUrl entry required!" .metadataServiceUrl }}
  FRONTEGG_HYBRID_SERVICES: {{ required "A valid .Values.frontegg.hybridServices entry required!" .hybridServices }}
  {{- end }}
