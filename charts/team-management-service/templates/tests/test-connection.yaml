apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "team-management-service.fullname" . }}-test-connection"
  labels:
    {{- include "team-management-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "team-management-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
